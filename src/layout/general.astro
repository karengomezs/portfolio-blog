---
import NavBar from "@/components/navBar.astro";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css"
      integrity="sha512-SzlrxWUlpfuzQ+pcUCosxcglQRNAq/DZjVsC0lE40xsADsfeQoEypE+enwcOiGjk/bSuGGKHEyjSoQ1zVisanQ=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <title>Karen Gómez</title>
  </head>
  <!-- <body class="bg-slate-900 container max-w-3xl mx-auto px-4 sm:px-10"> -->
  <body class="bg-slate-900 container px-5 mx-auto max-w-4xl">
    <NavBar />
    <main>
      <slot />
    </main>
    <style is:global>
      p {
        @apply text-xl text-justify mb-3 text-slate-300;
      }
      p a {
        @apply text-blue-500;
      }

      h3 {
        @apply text-2xl font-semibold text-white mb-8;
      }

      h2 {
        @apply text-3xl font-semibold text-white;
      }

      .hover-gradient {
        @apply text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-blue-500 hover:from-pink-500 hover:to-yellow-500;
      }

      .big-container {
        @apply flex flex-col mt-10 gap-4;
      }

      a button {
        @apply text-center font-semibold text-white rounded-md px-6 py-1 bg-gradient-to-r from-green-500 to-blue-500 hover:from-pink-500 hover:to-yellow-500;
      }

      /*
      .img-project {
        @apply object-fill h-full rounded-t sm:rounded-l sm:max-w-xs;
      }

      */
    </style>
  </body>
</html>
